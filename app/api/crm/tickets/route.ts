import {NextRequest,NextResponse} from "next/server";import {appendRow,getRows} from "@/lib/google/sheets-client";export async function GET(){try{const rows=await getRows("TICKETS");return NextResponse.json({rows})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Read failed"},{status:500})}}export async function POST(req:NextRequest){try{const t=await req.json();const id=t.ticketId||"SC-"+Date.now();await appendRow("TICKETS",[id,t.ticketType,t.subjectId||"",t.subjectName,t.reason,t.currentStep||1,t.currentAssignee,t.status||"PENDING_APPROVAL",new Date().toISOString(),new Date().toISOString(),"","",""]);return NextResponse.json({success:true,ticketId:id})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Save failed"},{status:500})}}