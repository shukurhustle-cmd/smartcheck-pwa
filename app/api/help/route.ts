import {NextRequest,NextResponse} from "next/server";import {appendRow} from "@/lib/google/sheets-client";export async function POST(req:NextRequest){try{const b=await req.json();const id="HELP-"+crypto.randomUUID();const now=new Date().toISOString();await appendRow("HELP_REQUESTS",[id,b.name||"",b.category||"",b.description||"",b.priority||"NORMAL","OPEN",now]);return NextResponse.json({success:true,requestId:id})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:"Help request failed"},{status:500})}}