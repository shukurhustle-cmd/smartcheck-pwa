import {NextRequest,NextResponse} from "next/server";export async function POST(req:NextRequest){const {type,rows}=await req.json();if(!type||!Array.isArray(rows))return NextResponse.json({error:"type and rows are required"},{status:400});return NextResponse.json({success:true,type,received:rows.length,message:"Validation complete. CRM persistence adapter is the next connection."})}