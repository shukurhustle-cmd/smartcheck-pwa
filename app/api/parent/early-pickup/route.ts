import {NextRequest,NextResponse} from "next/server";import {createTicket} from "@/lib/workflow/engine";import {appendRow} from "@/lib/google/sheets-client";export async function POST(req:NextRequest){try{const body=await req.json();const ticket=createTicket({ticketType:"EARLY_PICKUP",subjectName:body.studentName,reason:body.reason});await appendRow("TICKETS",[ticket.ticketId,ticket.ticketType,body.studentId||"",ticket.subjectName,ticket.reason,ticket.currentStep,ticket.currentAssignee,ticket.status,ticket.createdAt,ticket.createdAt,"","",""]);return NextResponse.json({success:true,ticket})}catch(error){return NextResponse.json({success:false,error:error instanceof Error?error.message:"Request failed"},{status:500})}}