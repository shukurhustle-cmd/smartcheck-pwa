import {NextRequest,NextResponse} from "next/server";import {appendRow} from "@/lib/google/sheets-client";export async function POST(req:NextRequest){try{const b=await req.json();const id="PAT-"+crypto.randomUUID();const now=new Date().toISOString();await appendRow("PATROLS",[id,b.officer||"",b.location||"",b.checkpoint||"",b.status||"COMPLETED",now,b.remarks||""]);return NextResponse.json({success:true,patrolId:id,timestamp:now})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:"Patrol save failed"},{status:500})}}