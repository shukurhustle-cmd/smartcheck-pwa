import {NextRequest,NextResponse} from "next/server";import QRCode from "qrcode";import {createQRToken} from "@/lib/qr/service";export async function POST(req:NextRequest){try{const {ticketId}=await req.json();if(!ticketId)return NextResponse.json({error:"ticketId required"},{status:400});const token=createQRToken(ticketId);const qrDataUrl=await QRCode.toDataURL(token,{margin:2,width:600});return NextResponse.json({ticketId,token,qrDataUrl})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"QR generation failed"},{status:500})}}