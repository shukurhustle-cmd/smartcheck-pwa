import {NextResponse} from "next/server";import {getRows} from "@/lib/google/sheets-client";import {summarizeTickets} from "@/lib/reports/daily";export async function GET(){try{const rows=await getRows("TICKETS");return NextResponse.json({success:true,report:summarizeTickets(rows)})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:"Report failed"},{status:500})}}