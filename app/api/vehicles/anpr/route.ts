import {NextRequest,NextResponse} from "next/server";import {recognizeVehicleNumber} from "@/lib/vehicle/anpr";export async function POST(req:NextRequest){try{const {image}=await req.json();if(!image)return NextResponse.json({error:"image required"},{status:400});return NextResponse.json(await recognizeVehicleNumber(image))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"ANPR failed"},{status:500})}}