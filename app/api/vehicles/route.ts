import {NextRequest,NextResponse} from "next/server";import {appendRow} from "@/lib/google/sheets-client";import {normalizeVehicleNumber,vehicleCaptureMetadata} from "@/lib/vehicle/service";export async function POST(req:NextRequest){try{const body=await req.json();const meta=vehicleCaptureMetadata();const number=normalizeVehicleNumber(body.vehicleNumber||"");await appendRow("VEHICLES",[meta.captureId,body.ticketId||"",number,body.photoUrl||"",meta.capturedAt]);return NextResponse.json({success:true,vehicleId:meta.captureId,vehicleNumber:number,...meta})}catch(error){return NextResponse.json({success:false,error:error instanceof Error?error.message:"Vehicle save failed"},{status:500})}}