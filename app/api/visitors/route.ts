import {NextRequest,NextResponse} from "next/server";import {appendRow} from "@/lib/google/sheets-client";import {createTicket} from "@/lib/workflow/engine";export async function POST(req:NextRequest){try{const b=await req.json();const ticket=createTicket({ticketType:"VISITOR",subjectName:b.visitorName,reason:b.purpose||"Visitor meeting"});await appendRow("TICKETS",[ticket.ticketId,"VISITOR","",ticket.subjectName,ticket.reason,ticket.currentStep,b.hostName||ticket.currentAssignee,ticket.status,ticket.createdAt,ticket.createdAt,"","",""]);return NextResponse.json({success:true,ticket,meeting:{hostName:b.hostName,roomNo:b.roomNo}})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:"Visitor request failed"},{status:500})}}