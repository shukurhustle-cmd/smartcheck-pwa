"use client";import {useState} from "react";export default function Help(){const [category,setCategory]=useState("Medical");const [description,setDescription]=useState("");const [message,setMessage]=useState("");async function submit(){const r=await fetch("/api/help",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({category,description,priority:category==="Emergency"?"HIGH":"NORMAL"})});const d=await r.json();setMessage(d.success?"Help request raised: "+d.requestId:d.error)}return <main style={{maxWidth:600,margin:"auto",padding:24}}><h1>Daily Help</h1><select value={category} onChange={e=>setCategory(e.target.value)} style={{width:"100%",padding:14}}><option>Medical</option><option>Maintenance</option><option>Lost & Found</option><option>Emergency</option><option>Other</option></select><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe the issue" style={{width:"100%",height:120,padding:14,marginTop:12}}/><button onClick={submit} style={{width:"100%",padding:14,marginTop:12,border:0,borderRadius:10,background:"#2563eb",color:"white"}}>Raise Help Request</button>{message&&<p>{message}</p>}</main>}