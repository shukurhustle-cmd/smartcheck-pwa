"use client";import {useState} from "react";export default function Parent(){const [student,setStudent]=useState("Ahmed Khan · Class 8A");const [reason,setReason]=useState("");const [result,setResult]=useState("");async function submit(){const r=await fetch("/api/parent/early-pickup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({studentName:student,reason})});const d=await r.json();setResult(d.success?"Request submitted: "+d.ticket.ticketId:d.error)}return <main style={{maxWidth:600,margin:"auto",padding:24}}><p style={{color:"#2563eb",fontWeight:700}}>PARENT PORTAL</p><h1>Request Early Pickup</h1><label>Student<select value={student} onChange={e=>setStudent(e.target.value)} style={{display:"block",width:"100%",padding:14,marginTop:7}}><option>Ahmed Khan · Class 8A</option></select></label><label style={{display:"block",marginTop:16}}>Reason<textarea value={reason} onChange={e=>setReason(e.target.value)} style={{display:"block",width:"100%",height:100,padding:12,marginTop:7}}/></label><button onClick={submit} disabled={!reason} style={{width:"100%",marginTop:20,padding:15,border:0,borderRadius:10,background:"#2563eb",color:"white"}}>Submit for Approval</button>{result&&<p style={{marginTop:16,fontWeight:700}}>{result}</p>}</main>}