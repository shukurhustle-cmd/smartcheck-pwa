"use client";import {useState} from "react";export default function Reports(){const [r,setR]=useState<any>();async function load(){const x=await fetch("/api/reports/daily");setR(await x.json())}return <main style={{maxWidth:600,margin:"auto",padding:24}}><h1>Daily Report</h1><p>Generate an operational summary for management sharing.</p><button onClick={load} style={{padding:14,border:0,borderRadius:10,background:"#2563eb",color:"white"}}>Generate Today's Report</button>{r?.report&&<div style={{background:"white",padding:20,borderRadius:14,marginTop:16}}><h2>{r.report.date}</h2><p>Total tickets: {r.report.total}</p><p>Closed: {r.report.closed}</p><p>Pending: {r.report.pending}</p><p>Rejected: {r.report.rejected}</p></div>}</main>}