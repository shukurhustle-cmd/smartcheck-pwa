import Link from "next/link";import {demoTickets} from "@/lib/store/demo";export default function Tickets(){return <main style={{maxWidth:700,margin:"auto",padding:24}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><h1>Tickets</h1><Link href="/tickets/new">+ Create</Link></div>{demoTickets.map(t=><Link href={"/tickets/"+t.ticketId} key={t.ticketId}><div style={{background:"white",padding:18,borderRadius:14,marginTop:12}}><b>{t.ticketType.replace("_"," ")}</b><p>{t.subjectName}</p><small>{t.status} · Assigned to {t.currentAssignee}</small></div></Link>)}</main>}