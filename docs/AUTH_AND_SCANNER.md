# Authentication and QR Scanning
Pilot roles: ADMIN, PARENT, VISITOR, APPROVER, RECEPTION, SECURITY.
Camera QR scanning uses the browser BarcodeDetector API and rear camera.
Before production, replace role picker with secure authentication and server-side authorization.