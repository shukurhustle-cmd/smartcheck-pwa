# Additional SmartCheck CRM Tabs

PATROLS
patrol_id, officer, location, checkpoint, status, timestamp, remarks

HELP_REQUESTS
request_id, name, category, description, priority, status, created_at

Add these tabs to the same Google Spreadsheet before enabling the corresponding modules.
