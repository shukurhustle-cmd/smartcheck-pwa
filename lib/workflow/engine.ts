import {Ticket,TicketStatus} from "@/types/ticket";export const WORKFLOWS={EARLY_PICKUP:[{step:1,role:"HEAD_ACADEMICS",action:"APPROVE"},{step:2,role:"RECEPTION",action:"VERIFY"},{step:3,role:"SECURITY",action:"CLEAR"}],VISITOR:[{step:1,role:"HOST",action:"APPROVE"},{step:2,role:"RECEPTION",action:"VERIFY"},{step:3,role:"SECURITY",action:"CLEAR"}]};export function createTicket(input:{ticketType:string;subjectName:string;reason:string}):Ticket{const workflow=WORKFLOWS[input.ticketType as keyof typeof WORKFLOWS]||WORKFLOWS.EARLY_PICKUP;return{ticketId:"SC-"+Date.now(),...input,currentStep:1,currentAssignee:workflow[0].role,status:"PENDING_APPROVAL",createdAt:new Date().toISOString()}}export function advanceTicket(ticket:Ticket):Ticket{const workflow=WORKFLOWS[ticket.ticketType as keyof typeof WORKFLOWS]||WORKFLOWS.EARLY_PICKUP;const next=workflow.find(x=>x.step===ticket.currentStep+1);if(!next)return{...ticket,status:"CLOSED",currentAssignee:"CLOSED",currentStep:ticket.currentStep+1};let status:TicketStatus=next.role==="RECEPTION"?"RECEPTION_PENDING":"SECURITY_PENDING";return{...ticket,currentStep:next.step,currentAssignee:next.role,status,qrToken:ticket.qrToken||crypto.randomUUID()}}